name: Tests

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v7
      - name: Test
        run: ./Scripts/test.sh
      - name: Build app bundle
        run: ./Scripts/build-app.sh
      - name: Verify bundle
        run: build/Hexa.app/Contents/MacOS/Hexa --verify-bundle
